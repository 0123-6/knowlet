{
  "name": "knowlet",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=24",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev:web": "pnpm --filter @knowlet/web dev",
    "build:web": "pnpm --filter @knowlet/web build",
    "typecheck": "pnpm -r typecheck"
  }
}
